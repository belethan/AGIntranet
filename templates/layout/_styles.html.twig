{# CSS commun (toujours chargé) #}
<link rel="stylesheet" href="{{ asset('styles/app.css') }}">

{# CSS FRONT #}
{% if asset_group is defined and asset_group == 'front' %}
    <link rel="stylesheet" href="{{ asset('styles/front.css') }}">
{% endif %}

{# CSS BACK / ADMIN #}
{% if asset_group is defined and asset_group == 'admin' %}
    <link rel="stylesheet" href="{{ asset('styles/admin.css') }}">
{% endif %}

{# CSS AUTH / LOGIN #}
{% if asset_group is defined and asset_group == 'auth' %}
    <link rel="stylesheet" href="{{ asset('styles/login.css') }}">
{% endif %}

{# Extension page spécifique #}
{% block page_stylesheets %}{% endblock %}

