<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Intranet AGDUC{% endblock %}</title>

    <link rel="icon" href="data:image/svg+xml,
            <svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22>
                <text y=%221.2em%22 font-size=%2296%22>⚫️</text>
                <text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text>
            </svg>"
    >

    <!-- BOOTSTRAP -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
          rel="stylesheet"
          crossorigin="anonymous">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css"
          rel="stylesheet">

    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    {% endblock %}

    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>

<body>

<!-- ====================== -->
<!--       L A Y O U T      -->
<!-- ====================== -->
<div class="layout">

    <!-- ======================== -->
    <!--   SIDEBAR VERTICAL GAUCHE -->
    <!-- ======================== -->
    <aside class="sidebar">

        <!-- Bloc haut (logo + image) -->
        <div class="sidebar-top">
            <img src="{{ asset('images/static/logoagduc.png') }}" alt="Logo" class="img-fluid logo-main">
            <span class="logo-accent"></span>
        </div>

        <!-- Bloc principal (menu + autre) -->
        <div class="sidebar-main">
            <!-- Titre INTRANET -->
            <div class="sidebar-section-title d-flex align-items-center">
                <span class="section-bar"></span>
                <span class="section-title">Intranet</span>
            </div>
            <!-- MENU -->
            <nav class="sidebar-menu mt-3">

                <!-- ITEM 1 -->
                <a href="#" class="sidebar-menu-item">
                    <img src="{{ asset('images/static/home.png') }}" alt="" class="sidebar-menu-icon">
                    <span>Espace Agduc</span>
                </a>

                <!-- ITEM 2 -->
                <a href="#" class="sidebar-menu-item">
                    <img src="{{ asset('images/static/folder.png') }}" alt="" class="sidebar-menu-icon">
                    <span>Annuaire</span>
                </a>

                <!-- ITEM 3 -->
                <a href="#" class="sidebar-menu-item">
                    <img src="{{ asset('images/static/user.png') }}" alt="" class="sidebar-menu-icon">
                    <span>Document</span>
                </a>

                <!-- ITEM 4 -->
                <a href="#" class="sidebar-menu-item">
                    <img src="{{ asset('images/static/user.png') }}" alt="" class="sidebar-menu-icon">
                    <span>Articles</span>
                </a>
                <!-- ITEM 5-->
                <a href="#" class="sidebar-menu-item">
                    <img src="{{ asset('images/static/user.png') }}" alt="" class="sidebar-menu-icon">
                    <span>Collaborations</span>
                </a>
                <!-- ITEM 6-->
                <a href="#" class="sidebar-menu-item">
                    <img src="{{ asset('images/static/user.png') }}" alt="" class="sidebar-menu-icon">
                    <span>Recrutement</span>
                </a>
                <!-- ITEM 6-->
                <a href="#" class="sidebar-menu-item">
                    <img src="{{ asset('images/static/user.png') }}" alt="" class="sidebar-menu-icon">
                    <span>Espace employé</span>
                </a>
                <!-- ITEM 7-->
                <a href="#" class="sidebar-menu-item">
                    <img src="{{ asset('images/static/user.png') }}" alt="" class="sidebar-menu-icon">
                    <span>Mes informations</span>
                </a>
                <!-- ITEM 8-->
                <a href="#" class="sidebar-menu-item">
                    <img src="{{ asset('images/static/user.png') }}" alt="" class="sidebar-menu-icon">
                    <span>Paramètres</span>
                </a>
            </nav>

            <div>
                <!-- Exemple : widget bas du menu -->
            </div>

        </div>

        <!-- Bloc bas (info user connecté) -->
        <div class="sidebar-bottom">
            <i class="bi bi-box-arrow-right fs-4"></i>
            <span>Déconnexion</span>
        </div>

    </aside>

    <!-- =================================== -->
    <!--    HEADER HORIZONTAL + PAGE CONTENT -->
    <!-- =================================== -->
    <div class="topbar-container">

        <!-- Header horizontal -->
        <div class="topbar">

            <div class="topbar-left">
                <p class="p1-bonjour">
                    Bonjour {{ app.user.prenom }} nous sommes ravis de vous accueillir !
                </p>
                <p class="p2-info">
                    Dans cet espace vous trouverez toutes les informations récentes de l’AGDUC.
                </p>
            </div>

            <div class="topbar-right">
                {% block header_actions %}
                    <button class="btn btn-primary">Action</button>
                    <button class="btn btn-secondary">Autre</button>
                {% endblock %}
            </div>

        </div>

        <!-- Contenu principal -->
        <main class="page-content">
            {% block page_content %}

            {% endblock %}
        </main>

    </div>

</div>

<!-- BOOTSTRAP JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        crossorigin="anonymous"></script>

</body>
</html>
